import {StyleSheet} from 'react-native';
import {scale} from '../../../../../customized/utils/scale';
import commonColors from '../../../../../../styles/colors';

const styles = StyleSheet.create({
  container: {       
    marginTop:scale(15),
    marginLeft:scale(233),
    position: 'absolute',
  }, 
});

export default styles;
